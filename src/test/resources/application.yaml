vcs:
  type: github
  url: api.github.com
  access_token: sometoken

security:
  use-cognito-token: false

logging:
  level:
    root: INFO

spring:
  profiles:
    active: test
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://someuserpool/someappid

server:
  error:
    include-message: always